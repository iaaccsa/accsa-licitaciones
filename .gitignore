# --- Secrets: NEVER commit ---
# Note: git does not accept trailing comments inside a pattern.
.env
.env.local
# OpenAI key consumed by graphify.sh
.env.graphify
# Mailgun SMTP password in plain text
smtp-config
# Supabase personal access token
.mcp.json
opencode.json
# Microsoft Graph OAuth cache
tools/planner/token_cache.bin
.claude/settings.local.json
infra/vm-credentials.md

# --- Independent projects, each with its own repository ---
accsa-chatbot-documentation/
accsa-admissibility-lab/

# --- Generated output and data dumps ---
# Per-machine runtime marker (PID + local proxy port), rewritten every session
.claude/.headroom_wrap_marker.json
graphify-out/
supabase-backup/
.tokensave/
tools/planner/venv/

# --- OS noise ---
.DS_Store
.serena/
